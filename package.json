{
  "name": "map-reduce",
  "version": "2.0.0",
  "description": "Fast map-reduce helper: tokenize text, classify by word length, count per category.",
  "type": "module",
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "lint-fix": "eslint . --fix",
    "test": "bun test --preload ./test/bun-preload.ts",
    "test-coverage": "bun test --preload ./test/bun-preload.ts --coverage --coverage-reporter=text",
    "build": "rm -rf dist && tsdown"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "bun-types": "^1.1.0",
    "eslint": "^9.0.0",
    "tsdown": "^0.22.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0"
  }
}
